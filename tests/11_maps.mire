load kioto

pub fn main: () {
    set m = [] :map[str i64] mut
    set m = dicts.set(m "a" 1)
    set m = dicts.set(m "b" 2)
    set m = dicts.set(m "c" 3)
    set val = dicts.get(m "a" 0)
    use dasu("val: {val}")
}