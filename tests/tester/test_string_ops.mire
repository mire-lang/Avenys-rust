load kioto

pub fn main: () {
    set s1 = "hello"
    set s2 = "world"
    set combined = s1 + " " + s2
    use dasu(combined)

    set len = combined => len()
    use dasu(len)

    set is_empty = combined => len() == 0
    use dasu(is_empty)
}