load kioto

fn factorial: (n :i64) :i64 {
    if n == 0 {
        return 1
    }
    return n * (factorial(n - 1))
}

pub fn main: () {
    set f5 = factorial(5)
    use dasu(f5)

    set f0 = factorial(0)
    use dasu(f0)

    set f10 = factorial(10)
    use dasu(f10)
}