load kioto

fn add: (a :i64, b :i64) :i64 {
    a + b
}

fn greet: (name :str) :str {
    "hello " + name
}

pub fn main: () {
    set sum = add(5, 3)
    use dasu(sum)

    set message = greet("world")
    use dasu(message)
}