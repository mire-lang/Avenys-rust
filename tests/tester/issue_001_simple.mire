load kioto

enum Result {
    Ok(value :i64)
    Err(msg :str)
}

pub fn main: () {
    set r = Result.Ok(42)

    set v = match r {
        Result.Ok(val) { val }
        Result.Err(e) { 0 }
    } :i64

    use dasu(v)
}