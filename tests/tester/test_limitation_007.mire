load kioto

struct Counter {
    value :i64
}

impl Counter {
    fn new: (initial :i64) :Counter {
        (Counter value: initial)
    }

    fn get_value: (self) :i64 {
        self.value
    }
}

pub fn main: () {
    set c = Counter::new(42)
    set v = c.get_value()
    use dasu(v)
}